plot(cldLORA_content,2)
plot(cldLORA_content,3)
plot(cldLORA_content,4)
plot(cldLORA_content,5)
plot(cldLORA_content,2)
plot(cldLORA_content,3)
plot(cldLORA_content,4)
cldLORA_process <- cld(LORA_profiling,timeInData =c(3,5,7))
kml(cldLORA_process,2:10,nbRedrawing = 15,toPlot="nothing")
plotAllCriterion(cldLORA_process)
plot(cldLORA_process,2)
plot(cldLORA_process,3)
plot(cldLORA_process,4)
rpart_cutoff(df=LORA_profiling,type="content")
rpart_cutoff(df=LORA_profiling,type="process")
handmade_cutoff(3,type="content")
handmade_cutoff(4,type="content")
handmade_cutoff(6,type="content")
handmade_cutoff(6,type="process")
handmade_cutoff(6,type="content")
rpart_cutoff <- function(df, type){
if(type=="process"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_process.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_process.1),
x_max = max(PAS_process.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot(step_data) +
geom_segment(aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2) +
geom_text(aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),  # adjust height
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(x = "PAS_process T1", y = "Mean GHQ score T1",
title = "Step Function from Tree Nodes with Bin Sizes") +
theme_minimal()
}
else if(type=="content"){
}
return(plot)
}
rpart_cutoff(df=LORA_profiling,type="process")
rpart_cutoff <- function(df, type){
if(type=="process"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_process.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_process.1),
x_max = max(PAS_process.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot(step_data) +
geom_segment(aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2) +
geom_text(aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),  # adjust height
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(x = "PAS_process T1", y = "Mean GHQ score T1",
title = "Step Function from Tree Nodes with Bin Sizes") +
theme_minimal()
}
else if(type=="content"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_content.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_content.1),
x_max = max(PAS_content.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot(step_data) +
geom_segment(aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2) +
geom_text(aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),  # adjust height
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(x = "PAS_content T1", y = "Mean GHQ score T1",
title = "Step Function from Tree Nodes with Bin Sizes") +
theme_minimal()
}
return(plot)
}
rpart_cutoff(df=LORA_profiling,type="content")
rpart_cutoff <- function(df, type){
if(type=="process"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_process.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_process.1),
x_max = max(PAS_process.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot() +
# Raw data points
geom_point(data = df, aes(x = PAS_process.1, y = ghq_sum.1), alpha = 0.3, color = "gray40", size = 1) +
# Step segments
geom_segment(data = step_data,
aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2, color = "steelblue") +
# Percentage labels
geom_text(data = step_data,
aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(
x = "PAS_process score at T1",
y = "Mean GHQ Score at T1",
title = "Step Function with Tree-Based Cutoffs and Raw Data"
) +
theme_minimal()
}
else if(type=="content"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_content.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_content.1),
x_max = max(PAS_content.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot() +
# Raw data points
geom_point(data = df, aes(x = PAS_content.1, y = ghq_sum.1), alpha = 0.3, color = "gray40", size = 1) +
# Step segments
geom_segment(data = step_data,
aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2, color = "steelblue") +
# Percentage labels
geom_text(data = step_data,
aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(
x = "PAS_content score at T1",
y = "Mean GHQ Score at T1",
title = "Step Function with Tree-Based Cutoffs and Raw Data"
) +
theme_minimal()
}
return(plot)
}
rpart_cutoff <- function(df, type){
if(type=="process"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_process.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_process.1),
x_max = max(PAS_process.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot() +
# Raw data points
geom_point(data = df, aes(x = PAS_process.1, y = ghq_sum.1), alpha = 0.3, color = "gray40", size = 1) +
# Step segments
geom_segment(data = step_data,
aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2, color = "steelblue") +
# Percentage labels
geom_text(data = step_data,
aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(
x = "PAS_process score at T1",
y = "Mean GHQ Score at T1",
title = "Step Function with Tree-Based Cutoffs and Raw Data"
) +
theme_minimal()
}
else if(type=="content"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_content.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_content.1),
x_max = max(PAS_content.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot() +
# Raw data points
geom_point(data = df, aes(x = PAS_content.1, y = ghq_sum.1), alpha = 0.3, color = "gray40", size = 1) +
# Step segments
geom_segment(data = step_data,
aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2, color = "steelblue") +
# Percentage labels
geom_text(data = step_data,
aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(
x = "PAS_content score at T1",
y = "Mean GHQ Score at T1",
title = "Step Function with Tree-Based Cutoffs and Raw Data"
) +
theme_minimal()
}
return(plot)
}
rpart_cutoff(df=LORA_profiling,type="content")
rpart_cutoff(df=LORA_profiling,type="process")
LORAr %>%
filter(t==1) %>%
dplyr::select(PAS_content, PAS_process) %>%
single_imputation() %>%
estimate_profiles(2,
variances = c("equal"),
covariances = c("equal"),nrep = 5) %>%
plot_profiles()
LPA_LORA_profiles <- LORAr %>%
filter(t==1) %>%
dplyr::select(PAS_content, PAS_process) %>%
single_imputation() %>%
estimate_profiles(2,
variances = c("equal"),
covariances = c("equal"),nrep = 5)
View(LPA_LORA_profiles)
LPA_LORA_profiles$model_3_class_2$dff$classes_number
LPA_LORA_profiles$model_3_class_2$dff$Class
table(LPA_LORA_profiles$model_3_class_2$dff$Class)
LORAr %>%
filter(t==1) %>%
dplyr::select(PAS_content, PAS_process) %>%
single_imputation() %>%
estimate_profiles(3,
variances = c("equal"),
covariances = c("equal"),nrep = 5) %>%
plot_profiles()
table(LPA_LORA_profiles$model_3_class_2$dff$Class)/622*100
LORAr %>%
filter(t==1) %>%
dplyr::select(PAS_content, PAS_process) %>%
single_imputation() %>%
estimate_profiles(2:6,
variances = c("equal","equal","varying"),
covariances = c("equal","zero","zero"),nrep = 5) %>%
compare_solutions(statistics = c("AIC","BIC"))
rpart_cutoff(df=LORA_profiling,type="content")
rpart_cutoff(df=LORA_profiling,type="process")
choice(cld3dPregTemp)
plotMeans3d(cld3dPregTemp,2)
plotAllCriterion(cldLORA_content)
plotAllCriterion(cldLORA_content)
kml(cldLORA_content,2:10,nbRedrawing = 15,toPlot="nothing")
plotAllCriterion(cldLORA_content)
plotAllCriterion(cldLORA_process)
plotAllCriterion(cldLORA_content)
plot(cldLORA_content,2)
plot(cldLORA_content,3)
plot(cldLORA_content,4)
handmade_cutoff(6,type="process")
handmade_cutoff(6,type="content")
rpart_cutoff <- function(df, type){
if(type=="process"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_process.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_process.1),
x_max = max(PAS_process.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot() +
# Raw data points
geom_point(data = df, aes(x = PAS_process.1, y = ghq_sum.1), alpha = 0.3, color = "gray40", size = 1) +
# Step segments
geom_segment(data = step_data,
aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2, color = "steelblue") +
# Percentage labels
geom_text(data = step_data,
aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(
x = "PAS_process score at T1",
y = "GHQ Score at T1",
title = "Step function with tree-based cutoffs and raw data",
) +
theme_minimal()
}
else if(type=="content"){
# Fit tree
tree <- rpart(ghq_sum.1 ~ PAS_content.1, data = df, control = rpart.control(cp = 0.01))
# Assign each observation to a terminal node and get prediction
df$node <- tree$where
df$prediction <- predict(tree)
# Total number of individuals
n_total <- nrow(df)
# Get bin info: prediction, range, count, and proportion
step_data <- df %>%
group_by(node, prediction) %>%
summarize(x_min = min(PAS_content.1),
x_max = max(PAS_content.1),
count = n(),
.groups = "drop") %>%
mutate(percentage = round(100 * count / n_total, 1)) %>%
arrange(x_min)
# Plot
plot <- ggplot() +
# Raw data points
geom_point(data = df, aes(x = PAS_content.1, y = ghq_sum.1), alpha = 0.3, color = "gray40", size = 1) +
# Step segments
geom_segment(data = step_data,
aes(x = x_min, xend = x_max,
y = prediction, yend = prediction),
size = 1.2, color = "steelblue") +
# Percentage labels
geom_text(data = step_data,
aes(x = (x_min + x_max) / 2,
y = prediction + 0.1 * sd(df$ghq_sum.1, na.rm = TRUE),
label = paste0(percentage, "%")),
size = 3.5, vjust = 0) +
labs(
x = "PAS_content score at T1",
y = "Mean GHQ Score at T1",
title = "Step Function with Tree-Based Cutoffs and Raw Data"
) +
theme_minimal()
}
return(plot)
}
rpart_cutoff(df=LORA_profiling,type="content")
rpart_cutoff(df=LORA_profiling,type="process")
tree <- rpart(ghq_sum.1 ~ PAS_process.1, data = LORA_profiling, control = rpart.control(cp = 0.01))
tree$where
unique(tree$where)
predict(tree)
plotMeans3d(cld3dPregTemp,2)
plotAllCriterion(cldLORA_content)
plot(cldLORA_content,2)
plot(cldLORA_content,3)
plot(cldLORA_content,4)
plot(cldLORA_content,5)
plot(cldLORA_content,7)
plotAllCriterion(cldLORA_process)
plot(cldLORA_process,2)
plot(cldLORA_process,3)
plot(cldLORA_process,4)
plot(LORAr$t,LORAr$PAS_content)
plot(LORAr$t,LORAr$PAS_content,type="l")
plot(LORAr$t,LORAr$PAS_process,type="l")
recode <- function(df,variables_to_recode,min,max){
for(variable in variables_to_recode){
inverse_var <- paste0(variable, "_inverse")
df[[inverse_var]] <- ifelse(df[[variable]] %in% min:max, max + min - df[[variable]], NA)
}
return(df)
}
# Function to select the data and build the sums
select_data <- function(df,list_week,variables_needed){
required_vars <- c(process_vars, content_vars)
# Filter to weeks of interest
df_week <- df %>% filter(t %in% weeks_needed)
# Keep only individuals with no missing values on required variables
ids_with_complete_data <- df_week %>%
group_by(id) %>%
summarise(all_answered = all(!is.na(unlist(across(all_of(required_vars))))),
.groups = "drop") %>%
filter(all_answered) %>%
pull(id)
# Check who is present in all weeks
ids_with_all_weeks_present <- df_week %>%
filter(id %in% ids_with_complete_data) %>%
group_by(id) %>%
summarise(n_weeks_present = sum(present), .groups = "drop") %>%
filter(n_weeks_present == length(weeks_needed)) %>%
pull(id)
# 4. Final data frame with selected individuals
df_selected <- df %>% filter(id %in% ids_with_all_weeks_present & t%in%weeks_needed)
# 5. Select the right variables
df_selected <- df_selected[,variables_needed]
# 6. Recode the variables
pas_torecode <- paste0("gpass_",c(1,8,10,13,16,18,20,26,27))
df_selected <- recode(df_selected,pas_torecode,min=1,max=4)
process_vars <- c(paste0("gpass_",c(1,8,10,13,16,18,20,26,27),"_inverse"),paste0("gpass_",c(2,6,11,12,14)))
# 7. Build the sums
df_selected[["PAS_content"]] <- rowSums(df_selected[,content_vars],na.rm = TRUE)
df_selected[["PAS_process"]] <- rowSums(df_selected[,process_vars],,na.rm = TRUE)
return(df_selected)
}
# Define weeks and required variables
weeks_needed <- c(1, 7, 13)
variables_needed <- colnames(LORA)
LORAr <- select_data(LORA, weeks_needed,variables_needed)
plot(LORAr$t,LORAr$PAS_process,type="l")
plot(LORAr$t,LORAr$PAS_content,type="l")
plot(cldLORA_content,2)
# Longitudinal study
## Packages ####
library(ggplot2)
library(dplyr)
library(rjags)
library(SSranef)
library(tidyr)
## Import the data and the functions ####
df <- readRDS("C:/Users/garan/Documents/Ecole/M1/Stage/Internship_repo/Longitudinal/LORA/ds_forJan.rds")
df <- readRDS("C:/Users/garan/Documents/Ecole/M1/Stage/Internship_repo/Classification/Longitudinal/LORA/ds_forJan.rds")
source("~/Ecole/M1/Stage/Internship_repo/Classification/Longitudinal/LORA/utils.R")
load("~/Ecole/M1/Stage/Internship_repo/Classification/Longitudinal/LORA/longitudinal_work_data.RData")
View(df_full_attendance)
## Packages ####
library(dplyr) # dataframe managment
library(ggplot2) # ploting
library(ggrepel)
library(car)  # For VIF
library(caret)  # for createFolds
library(randomForest)
library(tidyr) # dataframe management
library(gridExtra) # plotinh
library(haven) # read sav data
library(missForest) # to impute data
library(olsrr) # influence statistic
library(rpart) # classification trees
library(rstanarm) # bayesian lm
## Set-up ####
setwd("~/Ecole/M1/Stage/Internship_repo/Classification/Cross-sectional/RYSE data")
load("~/Ecole/M1/Stage/Internship_repo/Classification/Cross-sectional/clean_rdata.RData")
